"""llg-emulator: learn LLG magnetization dynamics for a thin film.

Kept import-light on purpose (no jax at package import) so that
``from llg_emulator.config import DEVICE`` can run before jax is imported.
"""
