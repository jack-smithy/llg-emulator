# Full training run (256 epochs).

seed = 0
epochs = 256
checkpoint_every = 8

[model]
hidden_channels = 32
num_blocks = 4
activation = "gelu"

[data]
size = "med"
batch_size = 256
viz_sample = "sp4/sample_0"

[optim]
name = "adam"
lr = 1e-3
